import Error from "../components/Error/Error";

/**
 * @function NoMatch functional component that renders an error message.
 * @return {JSX.Element} The rendered component.
 */

function NoMatch() {
    return ( 
        <>
            <main>
                <Error />
            </main>
        </> 
    );
};

export default NoMatch;